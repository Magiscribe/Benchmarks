The API is broken. Your job is to fix it.

Source code is in app/. The API runs in a container at localhost:8080.
Read the code, figure out what's wrong, fix it.

When you're done, create a .done file.
